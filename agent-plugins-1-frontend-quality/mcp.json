{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "frontend-quality": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/server/index.mjs"
      ],
      "env": {
        "QUALITY_PROFILE": "baseline"
      },
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
